import { IRoleAccessMask } from "../interfaces/role_access_mask";
export declare class RoleAccessMask implements IRoleAccessMask {
    Value: number | undefined;
    constructor(value: number | undefined);
}
